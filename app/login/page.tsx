import { LoginForm } from '@/features/Login';

const LoginPage = () => {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-24 bg-secondary-50">
      <LoginForm />
    </main>
  );
};

export default LoginPage;
